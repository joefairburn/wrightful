# 4.1 Polish bundle — finishing existing stubs

**Tier:** 4 (polish; small, high perceived quality) · **Status:** planned

Three independent, small wirings, all in `src/components/`.

## 4.1a Date-range presets

`runs-filter-bar.tsx` hardcodes a misleading `"Last 24 hours"` label but only supports a custom calendar range. Add a presets column (24h / 7d / 30d / this-month) inside `DateRangeFilter`'s popover calling the existing `onApply(fromIso, toIso)` (URL/refetch plumbing unchanged); fix the default label to be honest ("All time" or the real loader default — confirm in `runs-filters-where.ts`).

## 4.1b Density toggle

CSS (`.density-compact` in `styles.css`) exists but nothing toggles it. Clone `theme.ts` → `src/lib/density.ts` and `theme-toggle.tsx` → `DensityToggle` (place next to `ThemeToggle` in `sidebar-user-menu.tsx`); extend the FOUC boot script (`theme-init-script.ts`, injected by `middleware/01.head.ts`) to apply the class before paint.

## 4.1c Command-menu search

`CommandMenu` is built but fully commented out in `app-layout.tsx`, and lacks the promised recent-runs + global-test-search groups. Re-enable it (uncomment import + `useState` + `useCommandMenuShortcut` + JSX); add a session-authed `routes/api/t/[teamSlug]/p/[projectSlug]/search.ts` (recent runs by `createdAt` + test `title`/`file` LIKE via `searchFragment`/`escapeLike`); fetch on debounced input via TanStack Query (already in `QueryProvider`) into two new `CommandGroup`s that navigate to run / filtered test-catalog URLs. Reuse `useDebouncedValue`.

## Verification

- `pnpm check` + dashboard tests.
- Manual: ⌘K surfaces runs/tests and navigates; density persists across reload with no FOUC; presets set URL + refetch; range label accurate when unset.
