/**
 * Settings → Team root is server-side redirected to `./general` by
 * `index.server.ts`. This component never renders in normal use.
 */
export default function SettingsTeamRootRedirect() {
  return null;
}
